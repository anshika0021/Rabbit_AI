
  # File Upload SPA

  This is a code bundle for File Upload SPA. The original project is available at https://www.figma.com/design/8yFuCU2qeaNl0myCIxH2pk/File-Upload-SPA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  